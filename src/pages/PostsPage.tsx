import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

export default function PostsPage() {
  return (
    <Box>
      <Typography variant="h4">Posts</Typography>
      <Typography>블로그 게시물 목록 페이지입니다.</Typography>
    </Box>
  );
};